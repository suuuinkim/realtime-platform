spring:
  application:
    name: realtime-platform
  data :
    redis :
      host : localhost
      port : 6379
  jwt :
    secret : 'password-sikim'
    access-token-expiration : 3600000
    refresh-token-expiration : 1209600000